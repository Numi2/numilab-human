# Coupled vascular support requalification, 13 September 2026

The physical Apple M4 Pro owner was rebuilt from native commit
`cfef55a8199c2167fbe9beb04417a47db705db5d` (tree
`a842948af74698501546c46de98eb7b36277e528`). The repair closes a real
coupling failure exposed by the fullbody vascular package: the support solver
could publish a materially negative normal impulse without failing its
certificate, and the HumanIO receptor reducer consequently invalidated one
support group. The Matter certificate now fails closed for a negative accepted
normal impulse; the sensor projection clamps only a signed-zero numerical
residue (`1e-7` impulse units) before its weighted aggregation.

A regenerated exact fixture with 157 bodies, 129 coordinates, 128 generalized
velocities, 416 muscles, three Matter objects, twelve FEM nodes, one synthetic
vascular tissue owner, and a 12,500 ns step passes the coupled root and all ten
support receptor rows at proximal fixture pressure `0.0008`. The focused native
CTest selection passes 3/3 (`fullbody_bridge_prepared_root`,
`fullbody_authored_world`, and `fullbody_vascular_admission`). A deliberately
higher synthetic pressure (`0.001`) produces the previously observed negative
support impulse and now returns Matter status 6 without publishing an accepted
Human token. Both outcomes are retained in the evidence directory:

[`native-coupled-support-qualification/`](media/organ-blood-cavity-bridge-20260913/native-coupled-support-qualification/)

This is a native engineering requalification of a synthetic vascular owner on
the fullbody topology. It does not close anatomical registration, prescribed
organ supports or loading/unloading, calibrated force convergence, activation
calibration, unresolved material/density/boundary data, subject calibration,
two-way blood/tissue exchange, or sustained standing, recovery, or walking.
The existing package-admission receipt remains the source-of-truth for owner
identity and mass/momentum metadata; this receipt adds the dynamic support
boundary and preserves its unqualified scope.
