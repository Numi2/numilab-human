# Native Human source support-force admission — 2026-09-13

The physical M4 Pro run now transports the compiled source support reactions into the native Human stand owner. The ABI4 contact lane `.w` carries a finite source normal force in newtons; Metal converts that force through the registered point Jacobians when contact is enabled. The contact projection solves the unilateral normal row before the tangential friction rows, so a coupled inverse cannot turn a valid support reaction into a negative normal impulse.

The run used one source-bound adult Human package, the exact `12.5 µs` step, the NHCNT2 support primitive payload, NHEQ1 equalities, NHTENDON3, activation `1.0`, seven stance coordinate bounds, and six selected contact witnesses. On the physical Apple M4 Pro it compiled six active supports with `952.864475176 N` total reaction and a `1.86186775863e-6 N` maximum floating-root force residual. Contact-disabled one-step FP64 parity remained within `1.85241077788e-5` velocity error and `3.01470339803e-8` configuration error. The assistance-free 64-step transaction completed and replayed bitwise.

This does not close standing. The same accepted run reaches `2878.86621094 m/s²` peak acceleration, `0.199699997902` maximum velocity delta, `8.14668237581e-5` maximum configuration delta, and `1.55866828777e-7 m` maximum penetration. The static source balance and one-step parity therefore pass while temporal force convergence and sustained posture fail. The run is retained as partial evidence rather than promoted to a standing or walking result.

The source implementation is committed in the native branch as `9af1cf9d597fbb796bb20c370a8ca3c150f07b5a` (`Admit source support forces in Human stand solver`). The branch is clean; publishing to its HTTPS remote was attempted and rejected because the Mac mini had no GitHub credentials. The complete binary patch and logs are retained in [`Docs/media/native-support-force-20260913/`](media/native-support-force-20260913/).

Focused native checks passed: `numi_human_source_route_precision_metal`, `numi_human_support_precision`, `numi_human_behavior_telemetry`, and `numi_human_static_support` (`4/4`). The receipt separates source loading, contact projection, static equilibrium, parity, transaction, replay, and the failed sustained-standing gate; it does not qualify anatomical vessel walls, blood/tissue transfer, activation calibration, material calibration, subject calibration, recovery, or walking.
