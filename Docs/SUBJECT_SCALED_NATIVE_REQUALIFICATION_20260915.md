# Subject-scaled native replay requalification — 15 September 2026

The immutable NHRIGID2 handoff now runs through the published native probe on
the physical Apple M4 Pro. The exact source tuple is the public
`human-native-step281-rank-audit-20260915` build at commit
`337741b51bfc4a5552a837dacb4d0b5c4268d298`; its binary SHA-256 is recorded in
the [receipt](media/native-subject-scaled-runtime-20260915/receipt-v1.json).

The run consumes the patched 157-body input, the 416-route muscle payload,
NHTENDON3 tendon attachments, source support contacts, and 51 NHEQ1
equalities. It completes 64 steps at the canonical 12.5 µs clock with all 416
source muscles recruited, six active support contacts, zero penetration, no
root assistance, and bitwise replay. The patched binary closes the scalar mass
target at `65.5000000249 kg`; the bounded native support total is
`642.555027166 N`.

The native result remains a partial engineering requalification. It reports
`0.146436423063 m/s²` peak acceleration over the 0.8 ms horizon and a
`0.03299692183 N` maximum dynamic force-audit residual. The probe still uses
source support witnesses and a uniform mass-only candidate with fixed geometry;
it does not establish segment composition, geometry or inertia calibration,
full generalized force convergence, anatomical plantar loading, activation
calibration, organ/blood/tissue/fat/muscle ownership, sustained standing,
perturbation recovery, walking, or held-out subject prediction.

The raw native stdout/stderr and all input hashes are retained beside the
receipt. The compiler and immutable receipt are exposed by:

```sh
.numi/commands/human-subject-scaled-native-requalification \
  --receipt Docs/media/native-subject-scaled-runtime-20260915/receipt-v1.json
```
