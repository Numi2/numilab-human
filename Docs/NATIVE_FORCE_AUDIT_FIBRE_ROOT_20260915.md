# Native force audit after the fibre-root repair — 15 September 2026

The physical Mac mini M4 Pro reran the complete source-bound support
certificate after carrying the solved fibre lengths into the persistent
12.5 microsecond release. The native source is `f45fcfdc80a05c2226d638227295add7f789c55c` and the
binary is bound in the machine-readable receipt.

The audit publishes all 128 velocity-coordinate rows with six explicit
owners: gravity, muscle/tendon, joint equality, joint limit, support and
passive tissue. The reconstructed component sum differs from the native net by
at most `5.68e-14 N`; the maximum net residual is `2.99e-6 N`, the internal
residual RMS is `3.13e-6`, and six of ten source support witnesses carry
`952.864475319 N` against `952.864477038 N` body weight.

This closes the current static per-DoF audit for the repaired source state. It
does not close temporal force convergence: the same repair remains at
`0.115904301405 m/s²` over 0.8 ms and reaches `4.82679176331 m/s²` over 6.4 ms.
Anatomical collider admission, activation calibration, anatomical blood mass
transfer, calibrated materials and subject calibration remain open.

The receipt and all raw/derived artifacts are in
[`media/native-force-audit-fibre-root-20260915/`](media/native-force-audit-fibre-root-20260915/).
At the time of this audit, the source branch was a local Mac mini worktree and
publication was blocked by its configured GitHub HTTPS credential. That is no
longer the delivery state: the native source is publicly pinned by Numi Lab tag
`human-native-step281-rank-audit-20260915`
(`337741b51bfc4a5552a837dacb4d0b5c4268d298`) together with the exact five-input
Human package at tag `human-native-runtime-source-inputs-20260915`. The
[published runtime tuple](NATIVE_RUNTIME_SOURCE_PUBLICATION_20260915.md) retains
the reconstruction command and a fresh public-source bounded replay. The
historical binary hash and this audit's raw artifacts remain historical
provenance; the later source publication does not assert a byte-identical
rebuild of that executable.
