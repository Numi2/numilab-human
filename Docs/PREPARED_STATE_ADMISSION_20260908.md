# Prepared stance admission into the joint runtime

NumanX now admits the [compiled offline stance](COUPLED_EQUILIBRIUM_20260908.md)
through **v7/NHINIT1**, with NHCNT2 curved foot contact, NHEQ2 source equalities
and NHLIM1 source limits. The payload carries all 129 q, 128 v and 416 complete
muscle states at FP32 transport precision. Source rest coordinates remain
unchanged. It loads through the permanent Brain–Human–Matter transaction.

The qualification world contains three small pelvis-attached FEM samples and
12 attachments. Those samples are compiled at the prepared pose. This is a
bounded admission and transaction check; anatomical tissue registration,
loaded equilibrium, calibration, sustained standing/walking and performance
remain open. The existing costal timeout is not resolved by this increment.

## Permanent contract

Native `NumiHumanInitialState` owns encoding/decoding. The exact little-endian
NHINIT1 envelope binds the source archive, composed Human source, authored
Matter world and timestep. Each source-ordered muscle contributes excitation,
activation, fibre length in metres and fibre velocity in metres/second. All
scalars are finite; activation/excitation are bounded and fibre length is
strictly positive. Initial state is separate from source defaults, and later
steps continue the resident accepted state without host reset or restaging.

V7 checks exact bytes and fingerprint, dimensions, root quaternion, source,
world and clock. Tissue mass ownership establishes final frames before state
admission. Existing attachment position/velocity checks reject a cooked world
that does not match that state. Construction never relocates nodes or applies
offline NHEQ1 hard-stop reactions as external loads.

Brain supplies `PreparedInitialState` in `AuthoredMatterWorld`, and Gate C
accepts `--initial-state PATH --initial-state-fp HEX` with its existing authored
world, equality and limit arguments. The initial-state identity enters native
Human program/publication identity. No equilibrium claim follows from a
successful constructor.

## Curved-foot sensor repair

The first prepared run admitted and advanced physics but failed supplemental
validity and learning-record assertions. The sensor dispatch still assumed ten
physical support rows. NHCNT2 has 18 rows: two endpoints per capsule plus one
per heel ellipsoid. The all-channel dispatch check therefore invalidated all
five supplemental modalities. The earlier optional culture consumer would also
have read only the first ten physical rows.

The native loader now builds an immutable map of contiguous rows to the ten
source geometry receptors. A Metal reduction preserves total impulse and centre
of pressure, reports minimum separation and impulse-weighted slip velocity,
and invalidates a receptor when its source row is malformed. Unloaded geometry
uses its nearest witness. Supplemental sensing and culture consume this derived
view. The physical contact rows and force/acceptance authority stay with Matter.

The standalone GPU oracle verifies the complete 18-to-10 mapping, an analytic
171-unit total normal impulse, centre-of-pressure conservation, exact replay
and four malformed-row cases. The existing ten-row legacy transaction is also
rerun; physical receptor count and source geometry identity remain distinct.

## Evidence and remaining physical gates

The [receipt](media/prepared-state-20260908/receipt.json) binds native
`1ca0865` and Brain `916cc1c`, clean builds, source inputs, prepared payload,
the cooked fixture and raw logs. Six native tests and three Swift descriptor
tests pass. Prepared admission rejects omitted state and 14 payload/identity
mutations. Each clean transaction attempts nine physical candidates, publishes
eight roots over **0.8 ms**, explicitly rejects one candidate and replays the
physical token sequence and eight accepted q/v records exactly.

The independent verifier checks NHINIT1 against the original compiled q,
activation and fibre arrays byte for byte after FP32 transport. Accepted-state
records use compact little-endian FP32 values and a single bounded output write
to prevent diagnostic interleaving. Earlier runs with broken sensor masks and
interleaved state logs remain as historical evidence, without promotion.

The terminal source geometry check is retained as a **failure**: after 0.8 ms
the minimum primitive gap is **-1.9017 micrometres**, beyond that check's
one-micrometre admission tolerance. Source geometry lowering still agrees to
3.4135e-9 m. The largest source range excursion is 2.8775e-5 rad at left
`pm5_flexion`; source-compliant limits can admit range excursions, so this is a
measured displacement, not an assertion that the unilateral source law failed.
Right-ankle speed reaches 0.17134 rad/s at generation three; right-knee speed
is 0.11511 rad/s at generation eight. Loaded
settling, physical residual and convergence still require qualification.

```sh
python3 Docs/media/prepared-state-20260908/verify_receipt.py
python3 Docs/media/prepared-state-20260908/test_verify_receipt.py
```

This closes prepared-state plumbing and the curved-foot sensor mismatch. The
next physics gate is the accepted loaded trajectory: source-compliant joint,
contact and fibre equilibrium, timestep convergence and settling from this
state. The same initialization then needs registered anatomical tissue with
consistent frame/mass/prestrain ownership. Active control, sustained standing,
perturbation recovery and walking must pass the frozen behavior protocol on
that complete stack. The pre-contact Human acceleration diagnostic is not a
measurement of the final coupled equilibrium residual.
