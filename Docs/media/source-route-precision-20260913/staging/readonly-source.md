# Read-only paired source geometry

Only the three paths in source-sha256.json are proposed native source changes. patch.diff is against the captured base/ copies; no remote upload, build, GPU execution, commit, or publication was performed by this authoring task.

Nonempty MetalArticulatedOperatorInput.rootTranslations is the explicit opt-in outside Stand. Admission requires pointJacobiansOnly, a canonical floating-root record per environment exactly projecting to q.xyz, no Millard input and no resident continuation. Empty span preserves the legacy geometry path. Positive constitutive evaluation timestep remains required for private source muscle-state evaluation. No Stand horizon, physical step, accepted-root publication, clock advance, checkpoint or callback is created.

The reusable owner context handles both prepared paired geometry and the convenience API. The general C++ result exposes rootTranslations, bodyPositionLow and pointPositionLow; the existing standRootTranslations is still Stand-only. C++ dependent consumers must rebuild.

## Local validation

Both commands passed with exit code 0 and empty logs (core-syntax-final.log and probe-syntax-final.log):

```sh
xcrun clang++ -std=c++23 -fobjc-arc -fno-fast-math -ffp-contract=off -fsyntax-only -I/tmp/numi-readonly-paired-source-20260913/include -I/tmp/numi-readonly-paired-source-20260913/deps/include /tmp/numi-readonly-paired-source-20260913/src/metal/MetalArticulatedOperator.mm
xcrun clang++ -std=c++23 -Wall -Wextra -Wpedantic -Werror -fno-fast-math -ffp-contract=off -fsyntax-only -I/tmp/numi-readonly-paired-source-20260913/include -I/tmp/numi-readonly-paired-source-20260913/deps/include /tmp/numi-readonly-paired-source-20260913/apps/numilab_human_myosim_reference_probe.cpp
```

## Coordinated Mac mini qualification entry point

Build the existing metalrobo_numilab_human_myosim_reference_probe target. The new option is:

```sh
metalrobo_numilab_human_myosim_reference_probe MODEL.nhrigid MUSCLES.nhmyo --prepared-compensated-paths INITIAL.nhinit --timestep-us 100
```

Existing --prepared-paths is preserved. The new option includes six admission denials, same-context paired-repeat bitwise comparison, paired→legacy→paired cache transition, unchanged host source input records and no Stand outputs. It compares the actual paired geometry/source evaluation against the existing FP64 oracle with unchanged physical numerical thresholds. Runtime/GPU results remain pending parent qualification.
