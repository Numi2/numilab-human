# Exact 12.5 µs native clock receipt

The isolated Mac mini owner branch `human-clock-20260913` adds an additive
ABI8 runtime configuration and ABI2 physical-root request. The nested legacy
microsecond word is zero; `12,500 ns` is the sole timestep authority and the
runtime exposes a one-nanosecond clock quantum. The matching NHINIT2 state and
authored Matter package are admitted with source, joint-equality, joint-limit,
world, and initial-state fingerprints.

The physical M4 Pro probe passes the exact path through runtime construction,
HumanIO timing, motor-ready validation, one coupled Human/Matter physical
substep, prepared physical/culture receipts, accepted-state and support
diagnostics, and the existing unpublished-candidate timeout quarantine. The
legacy v1 root entry is rejected for an exact-clock runtime. The legacy
microsecond probe also passes after the additive change.

Native receipt: [manifest](media/exact-clock-20260913/native/manifest.json),
[exact probe log](media/exact-clock-20260913/native/exact-clock.log),
[legacy regression log](media/exact-clock-20260913/native/legacy-default.log),
and [SHA-256 manifest](media/exact-clock-20260913/native/SHA256SUMS).

Published native source revision: `3e63d1ca3beb7af0e8cb5da9a6394470fcc54b0b`.
The exact-clock result is a clock-admission and bounded transaction result. It
does not establish force or temporal convergence, two-way blood-mass momentum
closure, anatomical organ/vessel/neural registration, activation-data
calibration, unresolved material calibration, sustained standing, sustained
walking, or full release.
